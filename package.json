{
  "name": "munchtime-bot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "deploy": "CURRENT_BRANCH=`git rev-parse --abbrev-ref HEAD` && git push heroku-staging ${CURRENT_BRANCH}:master --force"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@slack/client": "^4.3.1",
    "@slack/events-api": "^1.0.1",
    "console.table": "^0.10.0",
    "express": "^4.16.3",
    "fortune-cookie": "0.0.1",
    "moment": "^2.22.2",
    "moment-timezone": "^0.5.21",
    "mongoose": "^5.2.3"
  },
  "devDependencies": {}
}
